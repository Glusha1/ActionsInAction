name: My First Workflow

on:
  push:
    branches:
      - main

jobs:
  MyFirstJob:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Hello"

      - name: Checkout
        uses: actions/checkout@v6.1.0

      - name: List files in repo
        run: |
          echo "repo ${{ github.repository }} contains: "
          tree
